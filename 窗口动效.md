1、概述
窗口动效通常分为两个部分：窗口动画和窗口效果。
1.1、窗口动画
1.1.1、定义
窗口动画通常是指按照既定的动画参数（动画曲线、时长、延时等）对窗口相关属性（可动画属性）进行逐帧修改从形成连贯的动画过程。在窗口中常见的动画是应用跳转动画，而应用跳转动画涉及了控件的位置、缩放、透明度等属性。

包括内容：熄屏（AOD）：来电。
锁屏：来电、拉起应用（比如相机）、应用跳转（相机跳转到图库）。
解锁：来电/去电、通话胶囊切前台、电话挂断、应用跳转、应用启动、应用退出、通话退后台、解锁。

当前明确划归窗口领域看护的动画场景有手机平台解锁状态下应用通过startAbility（可能还有类似startAbilityByType等接口）拉起另外应用的动画、锁屏/熄屏来电，而锁屏之上拉起应用（比如锁屏之上的相机拉起图库）有专门的窗口需求，当前由需求开发责任人看护，其他如点击图标启动、应用退出、通知中心拉起应用等都是大桌面其他模块自己定制的动画，故由大桌面看护。
在ets侧（SceneBoard仓）窗口动画（解锁状态下）的入口函数（属于生命周期）是SCBScenePanel::onSceneStartTransition和SCBScenePanel::onStartSceneFromOther，如果不是走这两个函数的跳转基本可以确定不是窗口领域看护的动画，另外即使走到了这两个函数也需要进一步确认内部逻辑是否走到了窗口动画（也有可能是桌面其他模块实现的）。（由此可知窗口动画与窗口生命周期密切相关。关于AMS是如何通过C++接口调用窗口生命周期不在本文说明范围。）
在锁屏状态下涉及窗口动效的入口函数是SCBSpecialScenePanel::addSpecialShowWhenLockedSceneContainerSession，其中动效逻辑是animationForPhoneCallScene，当前只在锁屏状态下才会触发该动效逻辑。
