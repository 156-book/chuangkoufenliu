概述
窗口控件是一个控件，拥有普通控件的各种属性，可以像普通控件那样对窗口控件进行状态变量的控制，同时承载了窗口的显示功能
显示其他应用内容的原理

控件：在计算机编程当中，控件（或部件，widget或control）是一种图形用户界面元素，其显示的信息排列可由用户改变，例如视窗或文本框。控件定义的特点是为给定数据的直接操作（direct manipulation）提供单独的互动点。
控件是一种基本的可视构件块，包含在应用程序中，控制着该程序处理的所有数据以及关于这些数据的交互操作。
控件的主要的作用是通过功能、事件等的封装来便于开发者构建UI和处理事件业务。

窗口控件：是一个控件，拥有普通控件的各种属性，可以像普通控件那样对窗口控件进行状态变量的控制，同时承载了窗口的显示功能，可以显示应用的具体内容，相当于一个Stack容器

​应用主窗​：负责看护窗口在各种场景下的正常显示、销毁

​启动页​：启动页的加载、显示、启动页内容布局、启动页消失动效

​截图快照​：截图获取、加载与显示

​蒙白​：窗口大小与上次大小不一致时作为过渡显示空白窗口

场景描述：

创建
全屏窗口启动退出
分屏窗口启动退出
悬浮窗口启动退出
应用间跳转
窗口形态变化
全屏窗口变分屏
全屏窗口变悬浮窗
分屏窗口变全屏
分屏窗口变悬浮窗
悬浮窗口变全屏
悬浮窗口变分屏
窗口控件和其他控件联动
窗口和应用图标
窗口和状态栏
窗口和导航栏
窗口和通知中心
窗口和卡片
窗口和壁纸
多任务管理
全屏进多任务
分屏进多任务
悬浮窗进多任务
多任务到全屏
多任务到分屏
多任务到悬浮窗
折叠屏折叠展开
